export interface menuItem {
    name: string;
    route: string;
}

export interface User {
    id: number;
    nombre: string;
    telefono: string;
    email: string;
    edad: number;
    isPremium: boolean;
}